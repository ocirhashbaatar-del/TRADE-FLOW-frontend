import { useState } from 'react'
import { Bell, Building2, KeyRound, Palette, Save, ShieldCheck, Users } from 'lucide-react'
import { PageHeader } from '@/components/common/page-header'
import { Button } from '@/components/ui/button'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select'
import { useTheme } from '@/contexts/theme-context'
import { usePageTitle } from '@/hooks/use-page-title'
const tabs = [{id:'workspace',label:'Workspace',icon:Building2},{id:'members',label:'Members & roles',icon:Users},{id:'security',label:'Security',icon:ShieldCheck},{id:'notifications',label:'Notifications',icon:Bell},{id:'appearance',label:'Appearance',icon:Palette},{id:'api',label:'API access',icon:KeyRound}]
export default function SettingsPage() { usePageTitle('Settings'); const [tab,setTab]=useState('workspace'); const {theme,setTheme}=useTheme(); return <><PageHeader eyebrow="Administration" title="Settings" description="Configure workspace identity, access, notifications, security, and integration preferences." actions={<Button><Save className="size-4"/>Save changes</Button>}/><div className="grid gap-5 lg:grid-cols-[240px_1fr]"><Card className="h-fit p-2">{tabs.map(item=><button key={item.id} onClick={()=>setTab(item.id)} className={`flex w-full items-center gap-3 rounded-xl px-3 py-2.5 text-left text-sm font-medium transition ${tab===item.id?'bg-brand-50 text-brand-700 dark:bg-brand-950/30 dark:text-brand-300':'text-slate-500 hover:bg-slate-100 dark:hover:bg-slate-800'}`}><item.icon className="size-4"/>{item.label}</button>)}</Card><Card><CardHeader><CardTitle>{tabs.find(item=>item.id===tab)?.label}</CardTitle><p className="text-sm text-slate-500">Reusable settings panel with mock persistence.</p></CardHeader><CardContent>{tab==='workspace'&&<div className="grid max-w-2xl gap-5 sm:grid-cols-2"><Field label="Workspace name" value="TradeFlow Global"/><Field label="Workspace URL" value="tradeflow-global"/><Field label="Industry" value="Supply Chain & Commerce"/><Field label="Primary region" value="North America"/><div className="sm:col-span-2"><Field label="Business address" value="120 Market Street, San Francisco, CA"/></div></div>}{tab==='appearance'&&<div className="max-w-2xl"><Label>Theme preference</Label><div className="mt-3 grid gap-3 sm:grid-cols-3">{(['light','dark','system'] as const).map(value=><button key={value} onClick={()=>setTheme(value)} className={`rounded-2xl border p-4 text-left transition ${theme===value?'border-brand-400 bg-brand-50 dark:bg-brand-950/30':'border-slate-200 dark:border-slate-800'}`}><div className={`h-24 rounded-xl border ${value==='dark'?'border-slate-700 bg-slate-950':value==='light'?'border-slate-200 bg-white':'bg-gradient-to-r from-white to-slate-950'}`}/><div className="mt-3 text-sm font-semibold capitalize">{value}</div></button>)}</div></div>}{!['workspace','appearance'].includes(tab)&&<div className="max-w-2xl space-y-5"><div className="rounded-2xl border border-slate-200 p-5 dark:border-slate-800"><div className="font-semibold">Default configuration</div><p className="mt-2 text-sm leading-6 text-slate-500">This section is ready for backend-provided tenant policy and user preferences.</p><div className="mt-4 flex items-center justify-between"><div><div className="text-sm font-medium">Enable enterprise policy</div><div className="mt-1 text-xs text-slate-400">Apply settings to all workspace members.</div></div><input type="checkbox" defaultChecked className="size-5"/></div></div><div className="space-y-2"><Label>Default option</Label><Select defaultValue="recommended"><SelectTrigger><SelectValue/></SelectTrigger><SelectContent><SelectItem value="recommended">Recommended</SelectItem><SelectItem value="strict">Strict</SelectItem><SelectItem value="custom">Custom</SelectItem></SelectContent></Select></div></div>}</CardContent></Card></div></> }
function Field({label,value}:{label:string;value:string}) { return <div className="space-y-2"><Label>{label}</Label><Input defaultValue={value}/></div> }
