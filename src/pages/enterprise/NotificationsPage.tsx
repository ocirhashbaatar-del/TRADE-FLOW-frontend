import { Bell, CheckCheck, Filter, Trash2 } from 'lucide-react'
import { PageHeader } from '@/components/common/page-header'
import { Button } from '@/components/ui/button'
import { Card } from '@/components/ui/card'
import { Badge } from '@/components/ui/badge'
import { useNotifications } from '@/contexts/notification-context'
import { usePageTitle } from '@/hooks/use-page-title'
export default function NotificationsPage() { usePageTitle('Notifications'); const {notifications,unread,markAllRead,markRead,clear}=useNotifications(); return <><PageHeader eyebrow="Activity center" title="Notifications" description="Review system, order, inventory, shipment, and team updates from one queue." actions={<><Button variant="secondary"><Filter className="size-4"/>Filter</Button><Button variant="secondary" onClick={markAllRead}><CheckCheck className="size-4"/>Mark all read</Button><Button variant="destructive" onClick={clear}><Trash2 className="size-4"/>Clear</Button></>}/><div className="mb-4 flex items-center gap-2"><Badge variant="blue">{unread} unread</Badge><span className="text-xs text-slate-400">Socket-ready mock notifications are added automatically.</span></div><Card className="divide-y divide-slate-100 dark:divide-slate-800">{notifications.length?notifications.map(item=><button key={item.id} onClick={()=>markRead(item.id)} className={`flex w-full gap-4 p-5 text-left transition hover:bg-slate-50 dark:hover:bg-slate-800/40 ${!item.read?'bg-brand-50/40 dark:bg-brand-950/10':''}`}><div className="grid size-10 shrink-0 place-items-center rounded-xl bg-white text-brand-600 shadow-sm ring-1 ring-slate-100 dark:bg-slate-900 dark:ring-slate-800"><Bell className="size-4"/></div><div className="flex-1"><div className="flex flex-wrap items-center gap-2"><span className="font-semibold">{item.title}</span>{!item.read&&<span className="size-2 rounded-full bg-brand-500"/>}</div><p className="mt-1 text-sm leading-6 text-slate-500">{item.description}</p><div className="mt-2 text-xs text-slate-400">{item.time}</div></div></button>):<div className="p-16 text-center text-sm text-slate-500">No notifications.</div>}</Card></> }
