import { useEffect, useState } from 'react'
import { useQuery } from '@tanstack/react-query'
import { Link, useNavigate, useParams, useSearchParams } from 'react-router-dom'
import { ArrowLeft, ArrowRight, Building2, Check, CheckCircle2, ChevronRight, CreditCard, Filter, Heart, Minus, PackageCheck, Plus, Search, ShieldCheck, ShoppingCart, SlidersHorizontal, Star, Store, Truck } from 'lucide-react'
import { motion } from 'framer-motion'
import { repositories } from '@/services/repositories'
import type { Product } from '@/types'
import { ProductGrid } from '@/features/marketplace/ProductGrid'
import { ProductCard } from '@/features/marketplace/ProductCard'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Badge } from '@/components/ui/badge'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Label } from '@/components/ui/label'
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select'
import { Skeleton } from '@/components/ui/skeleton'
import { currency } from '@/utils/format'
import { usePageTitle } from '@/hooks/use-page-title'

function useCategories() { return useQuery({ queryKey: ['marketplace-categories'], queryFn: () => repositories.marketplace.listCategories() }) }
function useCatalog() { return useQuery({ queryKey: ['marketplace-catalog'], queryFn: () => repositories.marketplace.listProducts() }) }

export function MarketplaceLandingPage() { usePageTitle('Marketplace landing'); const { data: catalog = [] } = useCatalog(); const { data: categories = [] } = useCategories(); return <><section className="overflow-hidden bg-slate-950"><div className="relative mx-auto grid max-w-[1500px] gap-10 px-4 py-20 sm:px-6 lg:grid-cols-2 lg:px-8 lg:py-28"><div className="absolute inset-0 bg-[radial-gradient(circle_at_25%_30%,rgba(37,99,235,.34),transparent_34%),radial-gradient(circle_at_85%_70%,rgba(99,102,241,.25),transparent_28%)]"/><div className="relative z-10 self-center"><Badge variant="blue" className="border-white/10 bg-white/10 text-blue-100 ring-white/10">Verified enterprise marketplace</Badge><h1 className="mt-6 text-balance text-4xl font-bold leading-[1.07] tracking-[-.045em] text-white sm:text-6xl">Source smarter. Fulfill faster. Operate together.</h1><p className="mt-6 max-w-xl text-lg leading-8 text-slate-300">Discover verified B2B products and suppliers connected directly to your TradeFlow operations workspace.</p><div className="mt-8 flex flex-wrap gap-3"><Button asChild size="lg"><Link to="/marketplace">Browse marketplace<ArrowRight className="size-4"/></Link></Button><Button asChild variant="secondary" size="lg"><Link to="/marketplace/vendor/atlas-components">Explore vendor stores</Link></Button></div><div className="mt-10 flex flex-wrap gap-6 text-sm text-slate-300">{['Verified vendors','Protected purchasing','Integrated fulfillment'].map((item) => <span key={item} className="flex items-center gap-2"><Check className="size-4 text-blue-400"/>{item}</span>)}</div></div><motion.div initial={{opacity:0,x:40}} animate={{opacity:1,x:0}} className="relative z-10 grid grid-cols-2 gap-4 self-center">{catalog.slice(0,4).map((product,index) => <div key={product.id} className={`overflow-hidden rounded-2xl border border-white/10 bg-white/10 p-3 backdrop-blur ${index%2?'translate-y-8':''}`}><img src={product.image} alt="" className="aspect-[4/3] w-full rounded-xl object-cover"/><div className="mt-3 text-sm font-semibold text-white">{product.name}</div><div className="mt-1 text-xs text-slate-300">{currency.format(product.price)}</div></div>)}</motion.div></div></section><section className="mx-auto max-w-[1500px] px-4 py-16 sm:px-6 lg:px-8"><div className="flex items-end justify-between"><div><div className="text-xs font-bold uppercase tracking-[.16em] text-brand-600">Explore categories</div><h2 className="mt-3 text-3xl font-bold tracking-tight">Built for modern supply chains</h2></div><Button asChild variant="ghost"><Link to="/marketplace/categories">All categories<ArrowRight className="size-4"/></Link></Button></div><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{categories.map((category) => <Link to={`/marketplace/search?q=${encodeURIComponent(category.name)}`} key={category.name} className="group rounded-2xl border border-slate-200 p-5 transition hover:-translate-y-0.5 hover:border-brand-300 hover:shadow-soft dark:border-slate-800"><div className="text-3xl">{category.icon}</div><div className="mt-5 flex items-center justify-between"><div><div className="font-semibold">{category.name}</div><div className="mt-1 text-xs text-slate-400">{category.count} products</div></div><ChevronRight className="size-5 text-slate-300 transition group-hover:translate-x-1 group-hover:text-brand-600"/></div></Link>)}</div></section></> }

export function MarketplacePage() { usePageTitle('Marketplace'); const [query,setQuery] = useState(''); const {data=[],isLoading} = useQuery({queryKey:['products',query],queryFn:()=>repositories.marketplace.listProducts(query)}); return <div className="mx-auto max-w-[1500px] px-4 py-10 sm:px-6 lg:px-8"><div className="rounded-3xl bg-gradient-to-br from-brand-700 via-brand-600 to-indigo-700 px-6 py-10 text-white sm:px-10"><div className="max-w-2xl"><Badge className="bg-white/15 text-white ring-white/20">Enterprise catalog</Badge><h1 className="mt-5 text-4xl font-bold tracking-[-.04em]">Marketplace</h1><p className="mt-3 text-blue-100">Search verified suppliers, compare operational products, and connect purchasing to your supply chain workflows.</p><div className="relative mt-7 max-w-xl"><Search className="absolute left-4 top-1/2 size-5 -translate-y-1/2 text-slate-400"/><Input value={query} onChange={(e)=>setQuery(e.target.value)} className="h-13 border-0 bg-white pl-12 text-slate-900" placeholder="Search products, categories, vendors..."/></div></div></div><div className="mt-8 flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="text-xl font-bold">Recommended products</h2><p className="mt-1 text-sm text-slate-500">Curated for your operation and purchase history</p></div><div className="flex gap-2"><Button variant="secondary"><Filter className="size-4"/>Filters</Button><Select defaultValue="recommended"><SelectTrigger className="w-44"><SelectValue/></SelectTrigger><SelectContent><SelectItem value="recommended">Recommended</SelectItem><SelectItem value="price-low">Price: Low to high</SelectItem><SelectItem value="rating">Highest rated</SelectItem></SelectContent></Select></div></div><div className="mt-6">{isLoading ? <div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{Array.from({length:8}).map((_,i)=><Skeleton key={i} className="h-96"/>)}</div> : <ProductGrid products={data}/>}</div></div> }

export function CategoriesPage() { usePageTitle('Categories'); const { data: categories = [] } = useCategories(); return <div className="mx-auto max-w-[1500px] px-4 py-12 sm:px-6 lg:px-8"><div className="max-w-2xl"><div className="text-xs font-bold uppercase tracking-[.16em] text-brand-600">Product discovery</div><h1 className="mt-3 text-4xl font-bold tracking-[-.04em]">All categories</h1><p className="mt-3 text-slate-500">Browse procurement-ready catalog groups created for industrial and enterprise purchasing.</p></div><div className="mt-10 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{categories.map((category,index)=><Link key={category.name} to={`/marketplace/search?q=${encodeURIComponent(category.name)}`} className="group relative overflow-hidden rounded-2xl border border-slate-200 p-6 transition hover:-translate-y-1 hover:shadow-float dark:border-slate-800"><div className="absolute -right-5 -top-5 text-8xl opacity-10">{category.icon}</div><div className="text-4xl">{category.icon}</div><h2 className="mt-8 text-lg font-semibold">{category.name}</h2><p className="mt-2 text-sm leading-6 text-slate-500">{category.count} verified products from enterprise-ready vendors.</p><div className="mt-5 flex items-center gap-2 text-sm font-semibold text-brand-600">Explore category<ArrowRight className="size-4 transition group-hover:translate-x-1"/></div></Link>)}</div></div> }

export function SearchPage() { const [params] = useSearchParams(); const initial = params.get('q')??''; const [query,setQuery]=useState(initial); usePageTitle('Marketplace search'); const {data=[]}=useQuery({queryKey:['product-search',query],queryFn:()=>repositories.marketplace.listProducts(query)}); return <div className="mx-auto max-w-[1500px] px-4 py-10 sm:px-6 lg:px-8"><div className="flex flex-col gap-6 lg:flex-row"><aside className="w-full lg:w-64"><div className="sticky top-24 rounded-2xl border border-slate-200 p-5 dark:border-slate-800"><div className="flex items-center gap-2 font-semibold"><SlidersHorizontal className="size-4"/>Advanced filters</div>{['Category','Vendor','Availability','Rating','Price range'].map((filter)=><div key={filter} className="mt-5 border-t border-slate-100 pt-4 dark:border-slate-800"><div className="text-sm font-medium">{filter}</div><div className="mt-3 space-y-2">{['Option one','Option two','Option three'].map((option)=><label key={option} className="flex items-center gap-2 text-xs text-slate-500"><input type="checkbox" className="rounded"/>{option}</label>)}</div></div>)}</div></aside><section className="min-w-0 flex-1"><h1 className="text-3xl font-bold">Search marketplace</h1><div className="relative mt-5"><Search className="absolute left-4 top-1/2 size-5 -translate-y-1/2 text-slate-400"/><Input value={query} onChange={(e)=>setQuery(e.target.value)} className="h-12 pl-12" placeholder="Search products..."/></div><div className="my-6 flex items-center justify-between"><p className="text-sm text-slate-500">{data.length} results for <strong className="text-slate-900 dark:text-white">“{query || 'all products'}”</strong></p><Select defaultValue="relevance"><SelectTrigger className="w-40"><SelectValue/></SelectTrigger><SelectContent><SelectItem value="relevance">Relevance</SelectItem><SelectItem value="newest">Newest</SelectItem><SelectItem value="price">Price</SelectItem></SelectContent></Select></div><ProductGrid products={data}/></section></div></div> }

export function ProductDetailsPage() { const {id='p-1'}=useParams(); const {data:product,isLoading}=useQuery({queryKey:['product',id],queryFn:()=>repositories.marketplace.getProduct(id)}); const { data: catalog = [] } = useCatalog(); const [qty,setQty]=useState(1); usePageTitle(product?.name??'Product'); if(isLoading)return <div className="mx-auto max-w-[1400px] px-4 py-10"><Skeleton className="h-[620px]"/></div>; if(!product)return <div className="p-20 text-center">Product not found.</div>; return <div className="mx-auto max-w-[1400px] px-4 py-10 sm:px-6 lg:px-8"><Link to="/marketplace" className="inline-flex items-center gap-2 text-sm text-slate-500 hover:text-brand-600"><ArrowLeft className="size-4"/>Back to marketplace</Link><div className="mt-6 grid gap-10 lg:grid-cols-2"><div><div className="overflow-hidden rounded-3xl bg-slate-100"><img src={product.image} alt={product.name} className="aspect-square w-full object-cover"/></div><div className="mt-4 grid grid-cols-4 gap-3">{Array.from({length:4}).map((_,i)=><button key={i} className={`overflow-hidden rounded-xl border-2 ${i===0?'border-brand-500':'border-transparent'}`}><img src={product.image} alt="" className="aspect-square object-cover"/></button>)}</div></div><div className="lg:py-4"><Badge variant="blue">{product.category}</Badge><h1 className="mt-5 text-balance text-4xl font-bold tracking-[-.045em]">{product.name}</h1><Link to={`/marketplace/vendor/${product.vendor.toLowerCase().replaceAll(' ','-')}`} className="mt-3 inline-flex items-center gap-2 text-sm text-slate-500 hover:text-brand-600"><Store className="size-4"/>Sold by <strong>{product.vendor}</strong></Link><div className="mt-5 flex items-center gap-3"><div className="flex items-center gap-1"><Star className="size-5 fill-amber-400 text-amber-400"/><span className="font-semibold">{product.rating}</span></div><span className="text-sm text-slate-400">{product.reviews} verified reviews</span></div><div className="mt-7 flex items-end gap-3"><span className="text-4xl font-bold">{currency.format(product.price)}</span>{product.compareAt&&<span className="pb-1 text-lg text-slate-400 line-through">{currency.format(product.compareAt)}</span>}</div><p className="mt-6 text-base leading-8 text-slate-600 dark:text-slate-300">{product.description}</p><div className="mt-6 flex flex-wrap gap-2">{product.tags.map((tag)=><Badge key={tag}>{tag}</Badge>)}</div><Card className="mt-8 p-5"><div className="flex items-center justify-between"><div><div className="font-semibold">Purchase quantity</div><div className="mt-1 text-xs text-emerald-600">{product.stock} units ready to ship</div></div><div className="flex items-center rounded-xl border border-slate-200 dark:border-slate-700"><Button variant="ghost" size="icon" onClick={()=>setQty(Math.max(1,qty-1))}><Minus className="size-4"/></Button><span className="w-10 text-center font-semibold">{qty}</span><Button variant="ghost" size="icon" onClick={()=>setQty(qty+1)}><Plus className="size-4"/></Button></div></div><div className="mt-5 grid gap-3 sm:grid-cols-[1fr_auto]"><Button size="lg"><ShoppingCart className="size-5"/>Add to cart · {currency.format(product.price*qty)}</Button><Button variant="secondary" size="lg"><Heart className="size-5"/></Button></div></Card><div className="mt-6 grid gap-3 sm:grid-cols-3">{[{ icon: Truck, title: 'Fast fulfillment', description: 'Ships in 1–2 days' }, { icon: ShieldCheck, title: 'TradeFlow protected', description: 'Verified supplier' }, { icon: PackageCheck, title: 'Easy returns', description: '30-day business return' }].map(({ icon: Icon, title, description })=><div key={title} className="rounded-xl border border-slate-200 p-3 dark:border-slate-800"><Icon className="size-5 text-brand-600"/><div className="mt-2 text-xs font-semibold">{title}</div><div className="mt-1 text-[11px] text-slate-400">{description}</div></div>)}</div></div></div><section className="mt-16"><div className="flex items-end justify-between"><div><h2 className="text-2xl font-bold">Recommended with this product</h2><p className="mt-1 text-sm text-slate-500">Operationally compatible products</p></div></div><div className="mt-6 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{catalog.filter((p)=>p.id!==product.id).slice(0,4).map((p,i)=><ProductCard key={p.id} product={p} index={i}/>)}</div></section></div> }

export function WishlistPage() { usePageTitle('Wishlist'); const { data: catalog = [] } = useCatalog(); return <CollectionPage title="Wishlist" description="Saved products ready for comparison or purchasing." products={catalog.slice(2,6)} icon={<Heart className="size-6"/>}/> }
export function RecommendationsPage() { usePageTitle('Recommendations'); const { data: catalog = [] } = useCatalog(); return <CollectionPage title="Recommendations for your team" description="Based on inventory patterns, recent searches, and supplier relationships." products={catalog.filter(p=>p.featured||p.rating>=4.7)} icon={<Star className="size-6"/>}/> }
function CollectionPage({title,description,products,icon}:{title:string;description:string;products:Product[];icon:React.ReactNode}) { return <div className="mx-auto max-w-[1500px] px-4 py-12 sm:px-6 lg:px-8"><div className="flex items-center gap-4"><div className="grid size-12 place-items-center rounded-2xl bg-brand-50 text-brand-600 dark:bg-brand-950/30">{icon}</div><div><h1 className="text-3xl font-bold tracking-tight">{title}</h1><p className="mt-1 text-sm text-slate-500">{description}</p></div></div><div className="mt-8"><ProductGrid products={products}/></div></div> }

export function CartPage() { usePageTitle('Cart'); const { data: catalog = [] } = useCatalog(); const [items,setItems]=useState<Array<Product & { qty: number }>>([]); useEffect(() => { if (catalog.length && !items.length) setItems(catalog.slice(0,3).map((product,index)=>({...product,qty:index+1}))) }, [catalog, items.length]); const subtotal=items.reduce((s,p)=>s+p.price*p.qty,0); return <div className="mx-auto max-w-[1300px] px-4 py-12 sm:px-6 lg:px-8"><h1 className="text-3xl font-bold">Your cart</h1><p className="mt-2 text-sm text-slate-500">Review quantities and purchasing details before checkout.</p><div className="mt-8 grid gap-6 lg:grid-cols-[1fr_360px]"><Card><CardContent className="divide-y divide-slate-100 p-0 dark:divide-slate-800">{items.map((item)=><div key={item.id} className="flex flex-col gap-4 p-5 sm:flex-row sm:items-center"><img src={item.image} alt="" className="size-24 rounded-xl object-cover"/><div className="min-w-0 flex-1"><div className="font-semibold">{item.name}</div><div className="mt-1 text-xs text-slate-500">{item.vendor} · {item.category}</div><div className="mt-3 font-bold">{currency.format(item.price)}</div></div><div className="flex items-center rounded-xl border border-slate-200 dark:border-slate-700"><Button variant="ghost" size="icon" onClick={()=>setItems(items.map(x=>x.id===item.id?{...x,qty:Math.max(1,x.qty-1)}:x))}><Minus className="size-4"/></Button><span className="w-10 text-center">{item.qty}</span><Button variant="ghost" size="icon" onClick={()=>setItems(items.map(x=>x.id===item.id?{...x,qty:x.qty+1}:x))}><Plus className="size-4"/></Button></div></div>)}</CardContent></Card><OrderSummary subtotal={subtotal}/></div></div> }
function OrderSummary({subtotal}:{subtotal:number}) { return <Card className="h-fit lg:sticky lg:top-24"><CardHeader><CardTitle>Order summary</CardTitle></CardHeader><CardContent><div className="space-y-3 text-sm"><div className="flex justify-between text-slate-500"><span>Subtotal</span><span>{currency.format(subtotal)}</span></div><div className="flex justify-between text-slate-500"><span>Estimated shipping</span><span>{currency.format(180)}</span></div><div className="flex justify-between text-slate-500"><span>Tax estimate</span><span>{currency.format(subtotal*.08)}</span></div><div className="flex justify-between border-t border-slate-200 pt-4 text-base font-bold dark:border-slate-800"><span>Total</span><span>{currency.format(subtotal*1.08+180)}</span></div></div><Button asChild size="lg" className="mt-6 w-full"><Link to="/marketplace/checkout">Proceed to checkout<ArrowRight className="size-4"/></Link></Button><div className="mt-4 flex items-center justify-center gap-2 text-xs text-slate-400"><ShieldCheck className="size-4"/>Secure enterprise purchasing</div></CardContent></Card> }

export function CheckoutPage() { usePageTitle('Checkout'); const navigate=useNavigate(); const { data: catalog = [] } = useCatalog(); const checkoutTotal = catalog.slice(0,3).reduce((sum, product) => sum + product.price, 180); return <div className="mx-auto max-w-[1200px] px-4 py-12 sm:px-6 lg:px-8"><div className="mb-8 flex items-center gap-3"><div className="grid size-10 place-items-center rounded-xl bg-brand-600 text-white"><CreditCard className="size-5"/></div><div><h1 className="text-3xl font-bold">Checkout</h1><p className="text-sm text-slate-500">Complete shipping and enterprise payment details.</p></div></div><div className="grid gap-6 lg:grid-cols-[1fr_360px]"><div className="space-y-5"><CheckoutSection title="Shipping address"><div className="grid gap-4 sm:grid-cols-2"><Field label="Contact name" value="Alex Morgan"/><Field label="Company" value="TradeFlow Global"/><Field label="Address" value="120 Market Street" span/><Field label="City" value="San Francisco"/><Field label="Postal code" value="94105"/></div></CheckoutSection><CheckoutSection title="Delivery method"><label className="flex items-center justify-between rounded-xl border border-brand-300 bg-brand-50 p-4 dark:bg-brand-950/20"><div className="flex items-center gap-3"><input type="radio" defaultChecked/><div><div className="font-semibold">Priority business delivery</div><div className="mt-1 text-xs text-slate-500">Estimated 2–4 business days</div></div></div><span className="font-semibold">$180</span></label></CheckoutSection><CheckoutSection title="Payment"><div className="grid gap-4 sm:grid-cols-2"><Field label="Cardholder name" value="Alex Morgan"/><Field label="Card number" value="4242 4242 4242 4242"/><Field label="Expiration" value="12/29"/><Field label="Security code" value="123"/></div></CheckoutSection></div><Card className="h-fit lg:sticky lg:top-24"><CardHeader><CardTitle>Purchase summary</CardTitle></CardHeader><CardContent><div className="space-y-4">{catalog.slice(0,3).map((p)=><div key={p.id} className="flex gap-3"><img src={p.image} alt="" className="size-12 rounded-lg object-cover"/><div className="min-w-0 flex-1"><div className="truncate text-xs font-semibold">{p.name}</div><div className="mt-1 text-xs text-slate-400">Qty 1</div></div><div className="text-xs font-semibold">{currency.format(p.price)}</div></div>)}</div><div className="mt-5 border-t border-slate-200 pt-5 dark:border-slate-800"><div className="flex justify-between font-bold"><span>Total</span><span>{currency.format(checkoutTotal)}</span></div></div><Button size="lg" className="mt-6 w-full" onClick={()=>navigate('/marketplace/order-success')}>Place order<Check className="size-4"/></Button></CardContent></Card></div></div> }
function CheckoutSection({title,children}:{title:string;children:React.ReactNode}) { return <Card><CardHeader><CardTitle>{title}</CardTitle></CardHeader><CardContent>{children}</CardContent></Card> }
function Field({label,value,span}:{label:string;value:string;span?:boolean}) { return <div className={`space-y-2 ${span?'sm:col-span-2':''}`}><Label>{label}</Label><Input defaultValue={value}/></div> }

export function OrderSuccessPage() { usePageTitle('Order complete'); return <div className="mx-auto flex min-h-[70vh] max-w-2xl items-center px-4 py-16 text-center"><div className="w-full"><motion.div initial={{scale:0}} animate={{scale:1}} transition={{type:'spring'}} className="mx-auto grid size-20 place-items-center rounded-full bg-emerald-100 text-emerald-600 dark:bg-emerald-950/40"><CheckCircle2 className="size-10"/></motion.div><Badge variant="green" className="mt-6">Order TF-10501</Badge><h1 className="mt-5 text-4xl font-bold tracking-[-.04em]">Order placed successfully</h1><p className="mx-auto mt-4 max-w-lg text-base leading-7 text-slate-500">Your purchase order has been created and the vendors have been notified. Live status updates are ready in your workspace.</p><div className="mt-8 flex flex-wrap justify-center gap-3"><Button asChild size="lg"><Link to="/marketplace/orders">View order<ArrowRight className="size-4"/></Link></Button><Button asChild variant="secondary" size="lg"><Link to="/marketplace">Continue shopping</Link></Button></div></div></div> }

export function OrderHistoryPage() { usePageTitle('Order history'); const { data: catalog = [] } = useCatalog(); return <div className="mx-auto max-w-[1300px] px-4 py-12 sm:px-6 lg:px-8"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><h1 className="text-3xl font-bold">Order history</h1><p className="mt-2 text-sm text-slate-500">Track marketplace purchases and vendor fulfillment.</p></div><Button variant="secondary"><Filter className="size-4"/>Filter orders</Button></div><div className="mt-8 space-y-4">{['TF-10492','TF-10490','TF-10488'].map((id,index)=><Card key={id} className="p-5"><div className="flex flex-col gap-5 sm:flex-row sm:items-center"><div className="grid size-12 place-items-center rounded-xl bg-brand-50 text-brand-600 dark:bg-brand-950/30"><PackageCheck className="size-6"/></div><div className="flex-1"><div className="flex flex-wrap items-center gap-2"><span className="font-semibold">{id}</span><Badge variant={index===0?'blue':'green'}>{index===0?'In transit':'Delivered'}</Badge></div><div className="mt-1 text-xs text-slate-500">Placed July {28-index*4}, 2026 · {index+2} vendor shipments</div><div className="mt-3 flex -space-x-2">{catalog.slice(index,index+3).map(p=><img key={p.id} src={p.image} className="size-9 rounded-full border-2 border-white object-cover dark:border-slate-900"/>)}</div></div><div className="sm:text-right"><div className="font-bold">{currency.format(18420-index*3200)}</div><Button variant="ghost" size="sm" className="mt-2">View details<ArrowRight className="size-4"/></Button></div></div></Card>)}</div></div> }

export function ReviewsPage() { usePageTitle('Reviews'); const { data: catalog = [] } = useCatalog(); return <div className="mx-auto max-w-[1200px] px-4 py-12 sm:px-6 lg:px-8"><h1 className="text-3xl font-bold">Product reviews</h1><p className="mt-2 text-sm text-slate-500">Verified buyer feedback across your recent purchases.</p><div className="mt-8 grid gap-5 lg:grid-cols-[340px_1fr]"><Card className="p-6 text-center"><div className="text-6xl font-bold">4.8</div><div className="mt-3 flex justify-center gap-1">{Array.from({length:5}).map((_,i)=><Star key={i} className="size-5 fill-amber-400 text-amber-400"/>)}</div><div className="mt-2 text-sm text-slate-500">Based on 428 verified reviews</div><div className="mt-7 space-y-3">{[5,4,3,2,1].map((r)=><div key={r} className="flex items-center gap-3 text-xs"><span>{r}★</span><div className="h-2 flex-1 rounded-full bg-slate-100 dark:bg-slate-800"><div className="h-full rounded-full bg-amber-400" style={{width:`${r===5?78:r===4?16:2}%`}}/></div><span className="w-7 text-right text-slate-400">{r===5?78:r===4?16:2}%</span></div>)}</div></Card><div className="space-y-4">{['Excellent quality and clear vendor communication.','The integration with purchasing made approval much faster.','Delivery was accurate and packaging was professional.'].map((text,index)=><Card key={text} className="p-5"><div className="flex items-center justify-between"><div><div className="font-semibold">{['Maya Chen','Noah Kim','Emma Davis'][index]}</div><div className="mt-1 text-xs text-slate-400">Verified enterprise buyer</div></div><div className="flex">{Array.from({length:5}).map((_,i)=><Star key={i} className="size-4 fill-amber-400 text-amber-400"/>)}</div></div><p className="mt-4 text-sm leading-6 text-slate-600 dark:text-slate-300">{text}</p><div className="mt-4 text-xs text-slate-400">Reviewed {catalog[index]?.name ?? 'TradeFlow product'}</div></Card>)}</div></div></div> }

export function VendorStorePage() { const {slug='atlas-components'}=useParams(); const { data: catalog = [] } = useCatalog(); const name=slug.split('-').map(x=>x[0].toUpperCase()+x.slice(1)).join(' '); usePageTitle(name); return <div><section className="border-b border-slate-200 bg-gradient-to-br from-slate-950 to-slate-800 text-white dark:border-slate-800"><div className="mx-auto max-w-[1500px] px-4 py-14 sm:px-6 lg:px-8"><div className="flex flex-col gap-6 sm:flex-row sm:items-center"><div className="grid size-20 place-items-center rounded-2xl bg-white text-2xl font-bold text-slate-900 shadow-float">AC</div><div className="flex-1"><div className="flex flex-wrap items-center gap-2"><h1 className="text-3xl font-bold">{name}</h1><Badge variant="blue"><Check className="mr-1 size-3"/>Verified</Badge></div><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-300">Industrial systems and certified components for high-performance supply chain operations.</p><div className="mt-4 flex flex-wrap gap-5 text-xs text-slate-300"><span>4.9 vendor rating</span><span>98% on-time shipping</span><span>Member since 2021</span></div></div><Button variant="secondary"><Building2 className="size-4"/>Contact vendor</Button></div></div></section><div className="mx-auto max-w-[1500px] px-4 py-12 sm:px-6 lg:px-8"><div className="flex items-end justify-between"><div><h2 className="text-2xl font-bold">Vendor catalog</h2><p className="mt-1 text-sm text-slate-500">Products supplied by {name}</p></div><Button variant="secondary"><Filter className="size-4"/>Filter</Button></div><div className="mt-6"><ProductGrid products={catalog.slice(0,6)}/></div></div></div> }
