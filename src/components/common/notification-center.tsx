import { Bell, Box, CheckCheck, PackageCheck, ShoppingCart, Truck } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { Sheet, SheetContent, SheetTrigger } from '@/components/ui/sheet'
import { useNotifications } from '@/contexts/notification-context'
const icons = { order: ShoppingCart, inventory: Box, shipment: Truck, system: PackageCheck, message: Bell }
export function NotificationCenter() { const { notifications, unread, markAllRead, markRead } = useNotifications(); return <Sheet><SheetTrigger asChild><Button variant="ghost" size="icon" className="relative" aria-label="Мэдэгдэл нээх"><Bell className="size-5"/>{unread > 0 && <span className="absolute right-1.5 top-1.5 grid size-4 place-items-center rounded-full bg-red-500 text-[9px] font-bold text-white">{unread}</span>}</Button></SheetTrigger><SheetContent title="Мэдэгдлийн төв"><div className="pr-8"><div className="flex items-center justify-between"><div><h2 className="text-lg font-bold">Мэдэгдлийн төв</h2><p className="mt-1 text-sm text-slate-500">Шууд ба системийн шинэчлэлт</p></div><Button variant="ghost" size="sm" onClick={markAllRead}><CheckCheck className="size-4"/>Бүгдийг уншсан болгох</Button></div><div className="mt-6 space-y-2">{notifications.map((item) => { const Icon = icons[item.type]; return <button key={item.id} onClick={() => markRead(item.id)} className={`w-full rounded-xl border p-3 text-left transition hover:bg-slate-50 dark:hover:bg-slate-800 ${item.read ? 'border-slate-100 dark:border-slate-800' : 'border-brand-200 bg-brand-50/60 dark:border-brand-900 dark:bg-brand-950/20'}`}><div className="flex gap-3"><div className="grid size-9 shrink-0 place-items-center rounded-lg bg-white text-brand-600 shadow-sm dark:bg-slate-900"><Icon className="size-4"/></div><div><div className="text-sm font-semibold text-slate-900 dark:text-white">{item.title}</div><p className="mt-1 text-xs leading-5 text-slate-500">{item.description}</p><div className="mt-1.5 text-[11px] text-slate-400">{item.time}</div></div></div></button>})}</div></div></SheetContent></Sheet> }
