import { ArrowDownRight, ArrowUpRight, Boxes, DollarSign, Gauge, ShoppingCart, Truck, Warehouse } from 'lucide-react'
import { Card } from '@/components/ui/card'
import type { Metric } from '@/types'
import { motion } from 'framer-motion'
const icons = { DollarSign, ShoppingCart, Boxes, Warehouse, Truck, Gauge }
export function MetricCard({ metric, index = 0 }: { metric: Metric; index?: number }) { const Icon = icons[metric.icon as keyof typeof icons] ?? Gauge; const positive = metric.trend === 'up'; return <motion.div initial={{ opacity: 0, y: 14 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: index * .045 }}><Card className="group overflow-hidden p-5 transition duration-300 hover:-translate-y-0.5 hover:shadow-float"><div className="flex items-start justify-between"><div className="grid size-10 place-items-center rounded-xl bg-brand-50 text-brand-700 transition group-hover:bg-brand-600 group-hover:text-white dark:bg-brand-950/50 dark:text-brand-300"><Icon className="size-5"/></div><div className={`flex items-center gap-1 text-xs font-semibold ${positive ? 'text-emerald-600' : 'text-red-600'}`}>{positive ? <ArrowUpRight className="size-3.5"/> : <ArrowDownRight className="size-3.5"/>}{metric.change}%</div></div><div className="mt-5 text-2xl font-bold tracking-tight text-slate-950 dark:text-white">{metric.value}</div><div className="mt-1 text-sm font-medium text-slate-700 dark:text-slate-200">{metric.label}</div><div className="mt-2 text-xs text-slate-400">{metric.helper}</div></Card></motion.div> }
