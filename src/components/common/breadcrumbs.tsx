import { ChevronRight, Home } from 'lucide-react'
import { Link, useLocation } from 'react-router-dom'
const pretty = (value: string) => value.split('-').map((part) => part[0]?.toUpperCase() + part.slice(1)).join(' ')
export function Breadcrumbs() { const { pathname } = useLocation(); const parts = pathname.split('/').filter(Boolean); if (!parts.length) return null; return <nav aria-label="Breadcrumb" className="mb-4 flex items-center gap-1.5 overflow-x-auto text-xs text-slate-500"><Link to="/dashboard" className="rounded p-1 hover:text-brand-600"><Home className="size-3.5"/></Link>{parts.map((part, index) => { const href = `/${parts.slice(0, index + 1).join('/')}`; const last = index === parts.length - 1; return <span key={href} className="flex items-center gap-1.5 whitespace-nowrap"><ChevronRight className="size-3.5 text-slate-300"/>{last ? <span className="font-medium text-slate-700 dark:text-slate-200">{pretty(part)}</span> : <Link to={href} className="hover:text-brand-600">{pretty(part)}</Link>}</span> })}</nav> }
