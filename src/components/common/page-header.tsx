import { motion } from 'framer-motion'
export function PageHeader({ title, description, eyebrow, actions }: { title: string; description?: string; eyebrow?: string; actions?: React.ReactNode }) { return <motion.div initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }} className="mb-6 flex flex-col justify-between gap-4 lg:flex-row lg:items-end"><div>{eyebrow && <div className="mb-2 text-xs font-bold uppercase tracking-[.16em] text-brand-600 dark:text-brand-400">{eyebrow}</div>}<h1 className="text-balance text-2xl font-bold tracking-[-.035em] text-slate-950 sm:text-3xl dark:text-white">{title}</h1>{description && <p className="mt-2 max-w-3xl text-sm leading-6 text-slate-500 sm:text-base dark:text-slate-400">{description}</p>}</div>{actions && <div className="flex flex-wrap items-center gap-2">{actions}</div>}</motion.div> }
